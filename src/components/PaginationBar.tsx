export default function PaginationBar({
  currentPage,
  setPage,
  totalPage
}: {
  currentPage: number
  setPage: (page: number) => void
  totalPage: number
}) {
  return (
    <div>
      
    </div>
  )
}

